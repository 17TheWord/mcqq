org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# Minecraft 26.1.2, on the Fabric toolchain as of 2026-09-20.
# The game is no longer obfuscated, so there is no mappings line to configure: sources use Mojang names.
# Loom and the shadow plugin carry their versions in build.gradle.kts, where a plugins block must name them.
minecraft_version=26.1.2
loader_version=0.19.5
fabric_api_version=0.155.3+26.1.2

mod_version=0.1.0
maven_group=com.example.mcqq
archives_base_name=mc-qq

# The SDK is a plain jar; its OkHttp/Gson/kotlin and SnakeYAML are relocated into each platform jar by shadow.
qqbot_sdk_version=0.0.4
snakeyaml_version=2.5
# The doubled platform the tests run against; must be the OkHttp line the SDK ships on.
mockwebserver_version=4.12.0
# What Minecraft ships; core only compiles against it.
slf4j_version=2.0.17
# Brigadier is Mojang's own library (not a Minecraft class), which is why the core may build command nodes
# with it. 1.3.10 is what Minecraft 26.1.2 ships; it lives on Mojang's libraries repo, not Maven Central.
brigadier_version=1.3.10

# The mod id, and it has to satisfy every loader at once:
#   NeoForge: ^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)*$  — no hyphens, so "mc-qq" is rejected outright
#   Fabric:   lowercase letters, digits, _ and - are all fine
#   Bukkit:   this is the plugin name, and also the name of its data folder
# "mcqq" is the one spelling all three accept, which is why the id is not the same as the project name.
mod_id=mcqq
# 描述符的公共字段：五个描述符（fabric.mod.json / 两个 mods.toml / 两个 plugin.yml）本来各写一遍，
# 现在只写在这里，由各自的 processResources expand 进去。平台的差异（比如 plugin.yml 那句"这是哪份 jar"）
# 仍然留在各自的描述符里。
mod_name=MC ↔ QQ Bot
mod_authors=SkiesWorld
mod_license=MIT
mod_url=https://github.com/skiesworld/mc-qq
mod_description=把聊天/进服/退服/死亡转发到 QQ 群，并把 QQ 群消息播进聊天栏（QQ 官方机器人 API v2）

# Forge. 26.1.2-64.1.3 = Minecraft 26.1.2, Forge build 64.1.3. Separate from NeoForge since 1.20.2.
forge_version=26.1.2-64.1.3
# What Forge's mods.toml demands of the loader: its own build number, not the game's.
forge_loader_version_range=[64,)

# NeoForge. Its version is four parts: 26.1.2.109 = build 109 for Minecraft 26.1.2.
neoforge_version=26.1.2.109
# What the descriptor demands of the loader itself (FML). NeoForge 26.1.2 ships 3.x, and its own descriptor
# says "[3,)" — so that is what a mod may ask for.
neoforge_loader_version_range=[3,)
# What the published versions claim on Modrinth / CurseForge — the window we actually ran on a server.
# It lives here, next to the other version facts, so bumping the window is one file. platforms.yml reads it.
publish_game_versions=[26.1, 26.2]

# The window one jar covers, spelled the way each descriptor wants it. Same claim as the Fabric side's
# `>=26.1`: the build targets 26.1.2, the range is what the file advertises.
minecraft_version_range=[26.1,)

# Paper's version scheme is `26.1.2.build.N-stable`, not `-R0.1-SNAPSHOT`. Compiling against paper-api gives
# the plugin both the plain Bukkit API and Paper's own events.
paper_api_version=26.1.2.build.74-stable
# Spigot's API only ships snapshots. The shared bukkit-common module compiles against this one
# (the lowest common denominator of the Bukkit family); Paper's API is a superset of it.
spigot_api_version=26.1.2-R0.1-SNAPSHOT

# `core` is compiled to this release on purpose. 21 is the lowest level that has virtual threads, which the
# bridge's dispatch wants; it also happens to cover both generations we care about, because Minecraft moved to
# Java 21 at 1.20.5 and to Java 25 at 26.1. Going down to 17 would buy the 1.18–1.20.4 line at the cost of
# virtual threads (and the reflection fallback that used to stand in for them).
core_java_release=21
