build/
.gradle/
run/
logs/
*.log
.idea/
# 测试服目录：`run/` 只匹配名字正好是 run 的目录，run-prod / run-folia 得单独写，
# 否则一次 `git add -A` 就会把几百 MB 的服务端推进仓库。
run-prod/
run-folia/
run-1.20.1/
# 工作笔记：按轮次的进展记录、规划稿、读别人项目的笔记。
# 它们记的是过程（谁提出、当时怎么想、验证输出了什么），不是别人读得懂的文档 ——
# docs/ 只放真正需要其他开发者读的东西（怎么加版本、模板怎么组织、为什么是这个形状）。
.notes/
