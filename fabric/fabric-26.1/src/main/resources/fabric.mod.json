{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": ["${mod_authors}"],
  "license": "${mod_license}",
  "environment": "*",
  "entrypoints": {
    "main": ["com.example.mcqq.fabric.McQqMod"]
  },
  "depends": {
    "fabricloader": ">=0.19.5",
    "fabric-api": ">=${fabric_api_version}",
    "minecraft": ">=26.1 <27",
    "java": ">=25"
  }
}
